"""
App configuration for the Menu app.
"""

from django.apps import AppConfig


class MenuConfig(AppConfig):
    """Configuration for the Menu app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "menu"
